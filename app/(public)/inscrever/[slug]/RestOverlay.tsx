// Temporary stub — replaced by Task 12 with the real "on rest" overlay.
import type { Championship } from "@/types/championship";

export default function RestOverlay(_props: { championship: Championship; liveCount: number }) {
  return null;
}
