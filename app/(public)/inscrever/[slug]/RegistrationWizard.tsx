// Temporary stub — replaced by Task 10 with the real registration wizard.
import type { Championship } from "@/types/championship";

export default function RegistrationWizard(_props: { championship: Championship; liveCount: number }) {
  return null;
}
